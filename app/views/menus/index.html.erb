<script>
  // make @food_carts variable available to react through the browser's window variable.
    window.menus = <%= raw(@menus.to_json) %>
</script>
<%= javascript_pack_tag 'Menus/index' %>
<% @foodcart.menu.food_item.each do |food_item| %>
  <%= food_item.name %>
<% end %>