<%# Map for the food carts %>
<% current_user = current_user != nil ? current_user : current_user = User.new(first_name: "guest", last_name: "user", email: "guest@guest.com", id: 'i love beer') %>
<%= render partial: 'shared/react_entry', locals: {data: {food_carts: @food_carts, schedules: @schedules, user: current_user, likes: @likes}.to_json, content_type: "FOOD_CARTS", search: @search} %>
