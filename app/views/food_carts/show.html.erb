<%= render partial: 'shared/react_entry', locals: {data: {food_cart: @food_cart.to_json(include: [:schedules, :menu, :food_items]), user: current_user, is_liked: current_user&.liked?(@food_cart), photo_key: @food_cart.photo.key, photos: @photos}.to_json, content_type: "FOOD_CART"} %>

<!-- Dynamic titles and descriptions for food cart -->
<% content_for :meta_title, "#{@food_cart.name} is on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, @food_cart.cart_description %>
<%# content_for :meta_image, cl_image_path(@food_cart.photo.key) %>
